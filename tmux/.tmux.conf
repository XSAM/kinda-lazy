# Common config
set -g repeat-time 1000  # default is 500
set-option -g set-titles on
set-option -g set-titles-string "#S / #W"
set-option -g history-limit 10000

set-window-option -g mode-keys vi

# Pane border
set-option -g pane-border-status top
# Note:
# `ps -t #{pane_tty} -o args= -o etime` Get tty cmd, and elapsed time
# `tail -n 1` Get last line
# `awk '{$NF=""}1'` Trim last word which is elapsed time field
# `sed 's/ *$//'` Remove whitespace from the end of line
# set-option -g pane-border-format " #P: #(sleep 0.25; ps -t #{pane_tty} -o args= -o etime | tail -n 1 | awk '{\$NF=\"\"}1' | sed 's/ *$//') "
if-shell "uname | grep -q Darwin" \
  "set-option -g pane-border-format ' #P: #(sleep 0.25; ps -t #{pane_tty} -o args= | tail -n 1) '"
if-shell "uname | grep -q Linux" \
  "set-option -g pane-border-format ' #P: #(sleep 0.25; ps -t #{pane_tty} -o args= --sort=etime | head -n 1) '"

# ---- Key Binding ----
# Move & Copy while scrolling
bind-key -T copy-mode-vi v send-keys -X begin-selection
bind-key -T copy-mode-vi y send-keys -X copy-selection-and-cancel
bind-key -T copy-mode-vi u send-keys -X halfpage-up
bind-key -T copy-mode-vi d send-keys -X halfpage-down

# Select pane
bind-key -r -T prefix j select-pane -D
bind-key -r -T prefix k select-pane -U
bind-key -r -T prefix h select-pane -L
bind-key -r -T prefix l select-pane -R

# Show history as scrollable view
# While a tmux pane is in “copy mode”, tmux dose not read any output from the processes running in the pane's tty,
# if the processes continue to write output to the tty, then the OS’s tty buffer will eventually fill. When a program
# writes to a tty with a full buffer, it causes the process to block.
# bind-key -T prefix F command-prompt 'capture-pane -S -10000 -b capture; show-buffer -b capture; send-keys G; delete-buffer -b capture'
# Without press enter key
bind F capture-pane -S -10000 -b capture\; show-buffer -b capture\; send-keys G\; delete-buffer -b capture
bind-key P command-prompt -p 'save history to filename:' -I '~/tmux.history' 'capture-pane -S -10000 -b capture ; save-buffer -b capture %1 ; delete-buffer -b capture'

# Parts of bind-key conflict with macOS and iTerm "Natural Text Editing"
# Use M-C-Up key will be overhandle by M-Up
bind-key -r -T prefix M-j resize-pane -D
bind-key -r -T prefix M-k resize-pane -U
bind-key -r -T prefix M-h resize-pane -L
bind-key -r -T prefix M-l resize-pane -R

bind-key -r -T prefix M-J resize-pane -D 5
bind-key -r -T prefix M-K resize-pane -U 5
bind-key -r -T prefix M-H resize-pane -L 5
bind-key -r -T prefix M-L resize-pane -R 5

# Split with current direcotry
bind '%' split-window -h -c '#{pane_current_path}'  # Split panes horizontal
bind '"' split-window -v -c '#{pane_current_path}'  # Split panes vertically
bind c new-window -c '#{pane_current_path}' # Create new window

# Fast session switching (ignore fzf-tmux exit code which happens when choose nothing
# bind-key -T prefix s run-shell "tmux list-sessions -F \"##S\" | fzf-tmux | xargs tmux switch -t; if [$? == "130" ];then exit 0;fi"
bind-key -T prefix s run-shell "tmux list-sessions -F \"##S\" | fzf-tmux | xargs tmux switch -t; true"
bind-key -T prefix S choose-tree -s
# ---- Key Binding ----

# Mouse configuration
# Double click sucks
# I just wanna scroll & select text in panel
# So if you wanna copy text in panel, type ':set mouse on' to temporarily turn on
# set-option -g mouse on

# Terminal type configuration
set -g default-terminal "screen-256color"
set -ga terminal-overrides ",xterm-256color:Tc"

# =---- Plugins ----=
set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'tmux-plugins/tmux-sensible'
set -g @plugin 'XSAM/tmux-themepack'

# Continuous saving tmux environments
set -g @plugin 'tmux-plugins/tmux-resurrect'
# set -g @plugin 'tmux-plugins/tmux-continuum'
# =---- Plugins ----=

# Initialize TMUX plugin manager (keep this line at the very bottom of tmux.conf)
run -b '~/.tmux/plugins/tpm/tpm'
